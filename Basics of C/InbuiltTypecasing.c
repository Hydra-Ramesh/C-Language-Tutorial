#include<stdio.h>
int main(){
    float a = 5.3;
    int b = (int)a;//(changing data type)the data or variable
    printf("%d",b);
    int c = 7;
    float d = (float)c;//(changing data type)the data or variable
    printf("\n%.2f",d);
    return 0;
}